[project]
name = "soma-client"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "eclipse-zenoh==1.6.2",
  "protobuf>=6,<8",
]

[project.optional-dependencies]
visualization = ["rerun-sdk==0.36.2"]

[tool.uv]
package = false
